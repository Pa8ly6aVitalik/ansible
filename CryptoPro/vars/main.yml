---
container_dir: lmelgkgu.000
dir_with_rpm: /tmp/cryptopro/linux-amd64/
tmp_dir: /tmp/cryptopro/
cert_dir: "{{tmp_dir}}certs/"
# Escape every slash ("\") by adding one extra slash ("\") 
container_name: '\\.\HDIMAGE\311092436'
user_name: user
user_cert: "{{cert_dir}}cert1.cer"
cryptopro_packages: [
"{{dir_with_rpm}}lsb-cprocsp-base*.deb",
"{{dir_with_rpm}}lsb-cprocsp-rdr-64*.deb",
"{{dir_with_rpm}}lsb-cprocsp-kc1.deb",
"{{dir_with_rpm}}lsb-cprocsp-capilite.deb",
"{{dir_with_rpm}}lsb-cprocsp-pkcs11.deb",
"{{dir_with_rpm}}cprocsp-rdr-gui-gtk*.deb"
]
root_cert: [
"{{cert_dir}}root.cer"
]